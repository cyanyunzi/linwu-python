import lombok.Data;
% for imp in table.imports:
import ${imp};
% endfor
/**
 * @author ：林雾
 * @description :${table.comment}护具传输对象
 */
@Data
public class ${table.className}DTO implements Serializable{
    % for field in table.fields:
        /**${field.comment}*/
        private ${field.javaType} ${field.javaName};
    % endfor
}
